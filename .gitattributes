# Do NOT use LFS for mp4 (GitHub Pages can't serve LFS objects)
# *.mp4 filter=lfs diff=lfs merge=lfs -text
# src/assets/hero/learn-to-skate-hero.mp4 filter=lfs diff=lfs merge=lfs -text

# Keep videos as normal git binaries (no filters)
public/videos/*.mp4 -filter -diff -merge binary
